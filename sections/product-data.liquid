{%- comment -%}
  This section is only used for section rendering API.
{%- endcomment -%}

{% render 'product-card',
  title: product.title,
  image: product.featured_image,
  swap_image: product.images[1],
  price: product.price,
  compare_at_price: product.compare_at_price,
  url: product.selected_or_first_available_variant.url,
  rating: product.metafields.custom.rating.value.rating,
  enable_quick_add_button: true,
  enable_wishlist_button: true,
  product: product,
  product_card_rating_prefix: 'product-card-template'
%}
